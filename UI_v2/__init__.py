"""Пакет другого покоління UI-шару для SMC viewer.

Містить:
- типізовані схеми (`schemas`);
- побудову агрегованого стану (`viewer_state_builder`);
- майбутні рендерери (`rich_viewer`, `rich_viewer_extended`).
"""
